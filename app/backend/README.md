# Express Backend

## Installation

In the project directory, run `npm install` to install express js dependencies

## Running

In the same directory, run `node index.js` to launch the backend locally
